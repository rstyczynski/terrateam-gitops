---
ansible_piepline:
  ansible_playbook: duck.yml
  control:
    python_info: true
    init_script: |
      ansible-galaxy install -r requirements.yml # will be not executed
      apt-get update
      apt-get install -y python3 python3-venv
      ansible-playbook venv_manager.yml


      
