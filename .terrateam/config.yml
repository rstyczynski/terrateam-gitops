
# cost estimation https://docs.terrateam.io/integrations/cost-estimation/#enabling-cost-estimation
cost_estimation:
  enabled: false

# hooks https://docs.terrateam.io/configuration-reference/hooks
hooks:
  plan:
    pre:
      - type: run
        cmd: ['.terrateam/hooks/pre_procedure.sh']
        run_on: always
    post:
      - type: run
        cmd: ['.terrateam/hooks/post_procedure.sh']
        run_on: always

dirs:
  terraform:
    tags: [TF_code]
    when_modified:
      file_patterns: ["${DIR}/*"]
  terraform/ansible:
    tags: [ANS_code]
    # layered run
    # https://docs.terrateam.io/advanced-workflows/layered-runs/
    when_modified:
      depends_on: 'dir:terraform'
      file_patterns: ["${DIR}/*"]
      # autoplan https://docs.terrateam.io/configuration-reference/when-modified
      autoplan: true

  ansible:
    tags: [ANS_code]
    # layered run
    # https://docs.terrateam.io/advanced-workflows/layered-runs/
    when_modified:
      # depends_on: 'dir:terraform'
      file_patterns: ["ansible/*"]
      # autoplan https://docs.terrateam.io/configuration-reference/when-modified
      autoplan: true

# workflows https://docs.terrateam.io/configuration-reference/workflows
workflows:
  - tag_query: TF_code
    engine:
      name: terraform
    plan:
      - type: init
      - type: plan
    apply:
      - type: init
      - type: apply
      - type: run
        cmd: ['../.terrateam/terraform/output.sh']

  - tag_query: ANS_code
    engine:
      name: custom
      init:    ['../.terrateam/ansible/init.sh']
      plan:    ['../.terrateam/ansible/plan.sh', '$TERRATEAM_PLAN_FILE']
      diff:    ['../.terrateam/ansible/diff.sh', '$TERRATEAM_PLAN_FILE']
      apply:   ['../.terrateam/ansible/apply.sh']
      outputs: ['../.terrateam/ansible/outputs.sh']
    plan:
      - type: init
      - type: plan
    apply:
      - type: init
      - type: apply